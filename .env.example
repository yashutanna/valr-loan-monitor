# VALR API Configuration
VALR_API_KEY=your_api_key_here
VALR_API_SECRET=your_api_secret_here

# Subaccount Configuration
LOAN_PRINCIPAL_SUBACCOUNT=your_principal_subaccount_id

# Loan Configuration
# Maintenance Margin Ratio = loan/collateral threshold for danger
# If your margin ratio (loan/collateral) exceeds this value, position is at risk of liquidation
# Example: 0.8 means if loan reaches 80% of collateral value, it's dangerous
MAINTENANCE_MARGIN_RATIO=0.8

# Monitoring Configuration
POLL_INTERVAL_MS=3600000
PORT=3000